# Changelog

## Runtime UI

- The runtime UI now labels passthrough mode as off, surfaces the `OVERTURE_SANDBOX` environment knob, and states that the runtime falls back to passthrough on the wrong platform, such as Linux without bwrap or macOS without krunkit.
