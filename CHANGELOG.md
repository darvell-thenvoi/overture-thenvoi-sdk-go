# Changelog

## Runtime UI

- The runtime UI now labels passthrough mode as off and surfaces the `OVERTURE_SANDBOX` environment knob.
- Operators should set `OVERTURE_SANDBOX=bwrap` on Linux and `OVERTURE_SANDBOX=krunkit` on macOS.
